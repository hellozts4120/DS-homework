4. 为判断图中是否存在环给出尽量多的方法，说明每一个方法的具体思路。

若图为无向图：
	1）若图中各顶点的度均大于或等于2，则图中必有环；
	2）对图进行DFS，此时图中的边只可能是树边或反向边，若一旦发现反向边，则表明图中必有环；

若图为有向图：
	1）若图中存在一拓扑排序，使得能够从一起点出发遍历图中所有顶点，则图中无环，反之则必定有环；
	2）若图中存在拓扑有序序列，则必定无环，反之则必定有环；
	3）若图的邻接矩阵为三角矩阵，则图中必定无环，反之可能有环。